{
  "name": "huddle-clients",
  "private": true,
  "workspaces": [
    "web",
    "desktop",
    "shared"
  ],
  "scripts": {
    "dev:web": "bun run --cwd web dev",
    "dev:desktop": "bun run --cwd desktop dev",
    "build": "bun run --cwd web build && bun run --cwd desktop build",
    "typecheck": "bun run --cwd web typecheck && bun run --cwd desktop typecheck",
    "lint": "bun run --cwd web lint && bun run --cwd desktop lint && bun run --cwd shared lint"
  }
}
